{
  "name": "mining-ore-sorting-m221-scada",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local web SCADA for a Schneider M221 mining ore-sorting line over Modbus TCP",
  "scripts": {
    "start": "node src/server.js",
    "control": "node src/start-control.js",
    "check": "node --check src/server.js && node --check src/config.js && node --check src/modbus-client.js && node --check src/plc.js && node --check src/tag-model.js && node --check src/start-control.js && node --check public/app.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
